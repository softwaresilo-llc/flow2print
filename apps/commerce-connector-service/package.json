{
  "name": "@flow2print/commerce-connector-service",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('commerce-connector-service: no tests yet')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@flow2print/service-kit": "workspace:*",
    "@nestjs/common": "^11.1.6",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}

