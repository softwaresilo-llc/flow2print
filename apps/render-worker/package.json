{
  "name": "@flow2print/render-worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('render-worker: no tests yet')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@flow2print/config": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}

