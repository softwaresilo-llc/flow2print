{
  "name": "@flow2print/render-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint 'src/**/*.ts'",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@flow2print/config": "workspace:*",
    "@flow2print/logging": "workspace:*"
  }
}
