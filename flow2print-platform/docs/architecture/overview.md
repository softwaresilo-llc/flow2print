# Flow2Print Overview

Flow2Print is split into:

- browser-facing applications
- HTTP domain services
- asynchronous workers
- shared contracts
- a separate Magento 2 connector

The bootstrap in this repository focuses on contract-first foundations rather than a one-off demo implementation.
